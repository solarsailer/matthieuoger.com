import {css} from 'styled-components'

// -------------------------------------------------------------
// Blockhint.
// -------------------------------------------------------------

export default css`
  position: absolute;
  top: -20px;
  right: 20px;

  color: black;
  opacity: 0.35;
  font-size: 1.15rem;
  font-weight: 500;
  text-transform: uppercase;
`
